import { createStore } from 'vuex';

const store = createStore({
  state: {
    // votre état initial
  },
  mutations: {
    // vos mutations
  },
  actions: {
    // vos actions
  },
  getters: {
    // vos getters
  },
});

export default store;
